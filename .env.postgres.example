# Sample environment for the local Postgres container
# Copy to .env.postgres and adjust as needed.

POSTGRES_DB=ragdb
POSTGRES_USER=rag
POSTGRES_PASSWORD=ragpassword
# Host port exposed by docker compose (change if 5432 is in use locally)
POSTGRES_HOST_PORT=5432

# Backend connectivity (inside compose network)
POSTGRES_SERVER=postgres_dev
POSTGRES_PORT=5432
POSTGRES_URL=postgresql+psycopg://rag:ragpassword@postgres_dev:5432/ragdb?sslmode=disable

# Local tooling & Alembic (host machine)
DATABASE_URL=postgresql+psycopg://rag:ragpassword@localhost:5432/ragdb

# Vector dimension for pgvector columns
PGVECTOR_DIM=768
